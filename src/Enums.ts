namespace mirage {
    export enum HorizontalAlignment {
        left = 0,
        center = 1,
        right = 2,
        stretch = 3,
    }
    export enum VerticalAlignment {
        top = 0,
        center = 1,
        bottom = 2,
        stretch = 3,
    }
    export enum Orientation {
        horizontal = 0,
        vertical = 1,
    }
}