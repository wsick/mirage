namespace mirage {
    export enum HorizontalAlignment {
        stretch = 0,
        left = 1,
        center = 2,
        right = 3,
    }
    export enum VerticalAlignment {
        stretch = 0,
        top = 1,
        center = 2,
        bottom = 3,
    }
    export enum Orientation {
        horizontal = 0,
        vertical = 1,
    }
}